<UserControl x:Class="CarCare.UserInterface.Components.Login"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CarCare.UserInterface.Components" xmlns:models="clr-namespace:CarCare.Processing.Models;assembly=CarCare.Processing" xmlns:converters="clr-namespace:CarCare.Processing.Converters;assembly=CarCare.Processing"
             mc:Ignorable="d"
             Loaded="Initialise"
             d:DesignHeight="275" d:DesignWidth="400">
    <UserControl.Resources>
        <converters:AuthenticationArgsConverter x:Key="AuthenticationArgsConverter"/>
    </UserControl.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.7*" />
            <RowDefinition Height="0.3*" />
        </Grid.RowDefinitions>

        <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled"
                      Grid.Row="0">
            <ScrollViewer.RenderTransform>
                <TranslateTransform x:Name="AccountsTransform" X="0" />
            </ScrollViewer.RenderTransform>
            <Grid>
                <Grid HorizontalAlignment="Center">
                    <ItemsControl ItemsSource="{Binding Users}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <local:UserLoginCard SelectUserCommand="{Binding DataContext.SelectUserCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}" />
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>

                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Horizontal"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                    </ItemsControl>
                </Grid>
            </Grid>
        </ScrollViewer>

        <StackPanel Orientation="Horizontal">
            <StackPanel.RenderTransform>
                <TranslateTransform x:Name="CredentialsTransform" X="400" />
            </StackPanel.RenderTransform>
            <StackPanel Orientation="Vertical" VerticalAlignment="Center"
                        Margin="20">
                <Button Command="{Binding ReturnToUsersListCommand}">←</Button>
                <Ellipse Width="60" Height="60">
                    <Ellipse.Fill>
                        <ImageBrush ImageSource="{Binding Avatar}" Stretch="UniformToFill"/>
                    </Ellipse.Fill>
                </Ellipse>
                <TextBlock Text="{Binding Username}" />
            </StackPanel>

            <StackPanel Orientation="Vertical" VerticalAlignment="Center">
                <StackPanel Orientation="Horizontal">
                    <PasswordBox Width="160" Height="20" x:Name="PasswordBox" />
                    <Button Command="{Binding LoginCommand}"
                            Width="60" Height="20" Margin="20">
                        <Button.CommandParameter>
                            <MultiBinding Converter="{StaticResource AuthenticationArgsConverter}">
                                <Binding RelativeSource="{RelativeSource AncestorType={x:Type Window}}"/>
                                <Binding ElementName="PasswordBox"/>
                            </MultiBinding>
                        </Button.CommandParameter>
                        Login
                    </Button>
                </StackPanel>

                <TextBlock Text="{Binding Error}" />
            </StackPanel>
            
        </StackPanel>

        <Grid Grid.Row="1">
            <StackPanel Orientation="Vertical" VerticalAlignment="Center">
                <TextBlock TextAlignment="Center" Margin="5" Width="200">
                    Don't have an account?
                </TextBlock>
                <Button Width="80" Margin="5"
                        Command="{Binding RegisterCommand}"
                        CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}">
                    Register
                </Button>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
