<UserControl x:Class="CarCare.UserInterface.Controls.DatePickerControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CarCare.UserInterface.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Border Style="{StaticResource InputControlBorder}">
        <StackPanel Orientation="Vertical" Margin="6">
            <TextBlock Style="{StaticResource InputControlHeading}" 
                       Text="{Binding Heading, RelativeSource={RelativeSource AncestorType={x:Type local:DatePickerControl}}}" />
            <DatePicker Style="{StaticResource InputControlDatePicker}"
                        SelectedDate="{Binding Date, RelativeSource={RelativeSource AncestorType={x:Type local:DatePickerControl}}, Mode=TwoWay}" />
        </StackPanel>
    </Border>
</UserControl>
